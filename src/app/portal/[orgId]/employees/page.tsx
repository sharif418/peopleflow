// /portal/[orgId]/employees — employee directory + CRUD
import { EmployeesView } from "@/components/portal/employees-view"

export default function PortalEmployeesPage() {
  return <EmployeesView />
}
